<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Campground</title>
</head>

<body>
  <h1>Edit this Campground</h1>
  <form action="/campgrounds/<%= campground._id %>?_method=PUT" method="POST">
    <div>
      <label for="title">Campground Title</label>
      <input type="text" name="campground[title]" id="title" placeholder="campground name" value="<%= campground.title %>">
    </div>
    <div>
      <label for="location">Campground Location</label>
      <input type="text" name="campground[location]" id="location" placeholder="campground location" value="<%= campground.location %>">
    </div>
    <button>Submit</button>
  </form>
  <h4><a href="/campgrounds/<%= campground._id %>">Back to Campground Information</a></h4>
  <h3><a href="/campgrounds">Back to List</a></h3>
</body>

</html>